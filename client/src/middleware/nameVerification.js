export function nameVerification(nameValidation) {
    if (nameValidation.length <= 0) {
      return "name must contain atleast One characters"
    } else {
      return "age verification successful"
    }
  }